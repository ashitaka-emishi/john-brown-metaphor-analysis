---
title: "Reproducibility Report"
description: "Status page for future reproducibility reporting."
---

No project reproduction report is available yet.

After migration, the baseline command will be:

```bash
python -m john_brown_research validate
pytest
quarto render .
```

Future reports must record the exact environment, commands, results, rendered
artifacts, source boundaries, and remaining limitations.
